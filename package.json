{
  "name": "ridge-root-cms",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@payloadcms/plugin-cloud-storage": "^3.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  },
  "scripts": {
    "typecheck": "node -e \"process.exit(require('fs').existsSync('tsconfig.json')?0:1)\" && tsc -p tsconfig.json --noEmit || echo 'TypeScript config not found'",
    "build": "echo 'CMS build is app-specific; ensure your runtime compiles TS or uses ts-node'",
    "start": "payload serve",
    "dev": "payload serve --watch"
  }
}
